import React from "react";
import TwitchChannel from "../../components/TwitchChannel";

const TwitchStream = () => {
  return (
    <div>
      <TwitchChannel channel="intersectamf" />
    </div>
  );
};

export default TwitchStream;
